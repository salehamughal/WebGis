console.log("Welcome to string topic")
let a ="Mughal";
let b= "saleha";
let c= "Limia";
// we can access the string's number individually using indexes
console.log(a[0])
// we can create the lower or bottom case of the string using to lowerCase and to upperCase
console.log(a.toLowerCase())
console.log(a.toUpperCase())
// we can count the length of the stirng
console.log(a.length)
// we can replace the string's number with any other number
console.log(a.replace("M","a"))
// We can replace string's "" to-> `` Following is the example
console.log(b + " Is the best friend of the " + c);
//  To->
console.log (`${b} is the best friend of the ${c}`);


